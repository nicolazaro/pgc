import React, { useState, useMemo } from 'react';
import { Search, Globe, Users, BookOpen, Building2, Briefcase, ChevronRight, ExternalLink, Filter, Database } from 'lucide-react';

// --- DATA ESTRUCTURADA PARA MODULARIDAD ---
// Para agregar países, simplemente añade una nueva clave con la estructura correspondiente.
const DATA_ECOSISTEMA = {
  "Argentina": {
    id: "ar",
    resumen: "Líder histórico en prospectiva latinoamericana con una fuerte base académica y técnica.",
    historia: "La prospectiva en Argentina tiene sus raíces en el Modelo Mundial Latinoamericano (Modelo Bariloche) de los años 70, una respuesta soberana al Club de Roma. Tras periodos de interrupción, hoy vive un renacimiento impulsado por la complejidad y la transformación digital.",
    secciones: [
      {
        id: "academia",
        titulo: "Ámbito Académico",
        icono: <BookOpen className="w-5 h-5" />,
        descripcion: "Programas de posgrado y centros de investigación que forman el núcleo teórico.",
        items: [
          { nombre: "UBA - Programa PAEF", detalle: "Actualización en Teoría y Práctica de la Prospectiva.", url: "https://sociales.uba.ar" },
          { nombre: "UNGS - Maestría MGCTI", detalle: "Gestión de la Ciencia, Tecnología e Innovación.", url: "https://ungs.edu.ar" },
          { nombre: "UNCUYO - CEP", detalle: "Centro de Estudios Prospectivos de Cuyo.", url: "https://cepcuyo.com" }
        ]
      },
      {
        id: "estado",
        titulo: "Organismos Públicos",
        icono: <Building2 className="w-5 h-5" />,
        descripcion: "Entidades técnicas que utilizan la anticipación para políticas de estado.",
        items: [
          { nombre: "CIECTI", detalle: "Investigación prospectiva para el diseño de políticas de CTI.", url: "https://ciecti.org.ar" },
          { nombre: "INTA - CIEP", detalle: "Prospectiva agroalimentaria y territorial.", url: "https://inta.gob.ar" },
          { nombre: "INTI", detalle: "Vigilancia tecnológica e Industria 4.0.", url: "https://inti.gob.ar" }
        ]
      },
      {
        id: "redes",
        titulo: "Redes y Cooperación",
        icono: <Users className="w-5 h-5" />,
        descripcion: "Nodos de intercambio internacional y comunidades de práctica.",
        items: [
          { nombre: "The Millennium Project", detalle: "Nodo argentino de la red global de futuros.", url: "https://millennium-project.org" },
          { nombre: "RIBER", detalle: "Red Iberoamericana de Prospectiva.", url: "https://redriber.org" },
          { nombre: "RAEF", detalle: "Red Argentina de Estudios de Futuro.", url: "https://proseres.com" }
        ]
      },
      {
        id: "privado",
        titulo: "Sector Consultoría",
        icono: <Briefcase className="w-5 h-5" />,
        descripcion: "Empresas que aplican gestión del conocimiento a la estrategia de negocios.",
        items: [
          { nombre: "Paradigma", detalle: "Transformación digital y planeamiento estratégico.", url: "https://paradigma.com" },
          { nombre: "Practia", detalle: "Gestión del cambio y marcos de agilidad.", url: "https://practia.global" },
          { nombre: "Midas", detalle: "Workshops estratégicos bajo incertidumbre.", url: "https://midasconsulting.com.ar" }
        ]
      }
    ],
    registros: [
      { tipo: "Investigador", entidad: "UBA", apellido: "Patrouilleau", nombre: "M. Mercedes", tema: "Prospectiva", ciudad: "CABA", area: "Planificación estratégica", nota: "Coord. Académica PAEF" },
      { tipo: "Investigador", entidad: "CIECTI", apellido: "Porta", nombre: "Fernando", tema: "CTI", ciudad: "CABA", area: "Política Tecnológica", nota: "Director Upstream" },
      { tipo: "Investigador", entidad: "UNCUYO", apellido: "Vitale", nombre: "Javier", tema: "Prospectiva", ciudad: "Mendoza", area: "Territorial/Agro", nota: "WFSF Executive Committee" },
      { tipo: "Investigador", entidad: "Millennium Project", apellido: "Gutiérrez", nombre: "Miguel A.", tema: "Prospectiva", ciudad: "CABA", area: "Seguridad/RRII", nota: "Director Nodo Latam" },
      { tipo: "Investigador", entidad: "CEP Cuyo", apellido: "Ragno", nombre: "Luis", tema: "Estudios Futuro", ciudad: "Mendoza", area: "Educación", nota: "Presidente CEP Cuyo" },
      { tipo: "Investigador", entidad: "UNGS", apellido: "Yoguel", nombre: "Gabriel", tema: "Economía", ciudad: "Buenos Aires", area: "Sistemas de Innovación", nota: "Ref. Economía Innovación" },
      { tipo: "Empresa", entidad: "Paradigma", apellido: "-", nombre: "-", tema: "Estrategia", ciudad: "CABA", area: "Transformación Digital", nota: "Consultoría de negocios" }
    ]
  }
};

const App = () => {
  const [paisActivo, setPaisActivo] = useState("Argentina");
  const [searchTerm, setSearchTerm] = useState("");
  const [filtroTipo, setFiltroTipo] = useState("Todos");

  const data = DATA_ECOSISTEMA[paisActivo];

  const filteredRegistros = useMemo(() => {
    return data.registros.filter(r => {
      const matchSearch = Object.values(r).some(val => 
        val.toLowerCase().includes(searchTerm.toLowerCase())
      );
      const matchTipo = filtroTipo === "Todos" || r.tipo === filtroTipo;
      return matchSearch && matchTipo;
    });
  }, [searchTerm, filtroTipo, data]);

  return (
    <div className="min-h-screen bg-slate-50 font-sans text-slate-900">
      {/* Navbar */}
      <nav className="bg-white border-b border-slate-200 sticky top-0 z-50">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="flex justify-between h-16 items-center">
            <div className="flex items-center gap-2">
              <Globe className="text-blue-600 w-8 h-8" />
              <span className="text-xl font-bold tracking-tight text-slate-800">
                Observatorio <span className="text-blue-600">Prospectiva</span>
              </span>
            </div>
            <div className="flex gap-4">
              {Object.keys(DATA_ECOSISTEMA).map(pais => (
                <button
                  key={pais}
                  onClick={() => setPaisActivo(pais)}
                  className={`px-3 py-1 rounded-full text-sm font-medium transition-colors ${
                    paisActivo === pais 
                    ? "bg-blue-100 text-blue-700" 
                    : "text-slate-500 hover:text-slate-800"
                  }`}
                >
                  {pais}
                </button>
              ))}
            </div>
          </div>
        </div>
      </nav>

      {/* Hero Section */}
      <header className="bg-white border-b border-slate-200 py-12">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <h1 className="text-4xl font-extrabold text-slate-900 mb-4">
            Relevamiento: {paisActivo}
          </h1>
          <p className="text-lg text-slate-600 max-w-3xl leading-relaxed">
            {data.resumen}
          </p>
          <div className="mt-8 p-6 bg-blue-50 rounded-2xl border border-blue-100">
            <h2 className="text-sm font-bold uppercase tracking-wider text-blue-800 mb-2 flex items-center gap-2">
              <Database className="w-4 h-4" /> Contexto Histórico y Razonamiento
            </h2>
            <p className="text-blue-900/80 leading-relaxed italic">
              "{data.historia}"
            </p>
          </div>
        </div>
      </header>

      <main className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12 space-y-16">
        
        {/* Categorías Principales */}
        <section>
          <h3 className="text-2xl font-bold mb-8 flex items-center gap-2">
            Mapeo del Ecosistema
          </h3>
          <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
            {data.secciones.map((sec) => (
              <div key={sec.id} className="bg-white p-6 rounded-xl border border-slate-200 shadow-sm hover:shadow-md transition-shadow">
                <div className="p-2 bg-slate-100 rounded-lg w-fit mb-4">
                  {sec.icono}
                </div>
                <h4 className="font-bold text-lg mb-2">{sec.titulo}</h4>
                <p className="text-sm text-slate-500 mb-6">{sec.descripcion}</p>
                <ul className="space-y-3">
                  {sec.items.map((item, idx) => (
                    <li key={idx}>
                      <a 
                        href={item.url} 
                        target="_blank" 
                        rel="noopener noreferrer"
                        className="group flex flex-col hover:bg-slate-50 p-2 -mx-2 rounded-lg transition-colors"
                      >
                        <span className="text-sm font-semibold flex items-center justify-between text-blue-600 group-hover:text-blue-800">
                          {item.nombre} <ExternalLink className="w-3 h-3" />
                        </span>
                        <span className="text-xs text-slate-400">{item.detalle}</span>
                      </a>
                    </li>
                  ))}
                </ul>
              </div>
            ))}
          </div>
        </section>

        {/* Base de Datos Granular */}
        <section className="bg-white rounded-2xl border border-slate-200 shadow-sm overflow-hidden">
          <div className="p-8 border-b border-slate-100 bg-slate-50/50">
            <div className="flex flex-col md:flex-row md:items-center justify-between gap-6">
              <div>
                <h3 className="text-2xl font-bold">Investigadores y Entidades</h3>
                <p className="text-slate-500">Datos granulares obtenidos del relevamiento nacional.</p>
              </div>
              <div className="flex flex-col sm:flex-row gap-4">
                <div className="relative">
                  <Search className="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-slate-400" />
                  <input
                    type="text"
                    placeholder="Buscar por nombre, área..."
                    className="pl-10 pr-4 py-2 bg-white border border-slate-200 rounded-lg text-sm w-full sm:w-64 focus:ring-2 focus:ring-blue-500 outline-none"
                    value={searchTerm}
                    onChange={(e) => setSearchTerm(e.target.value)}
                  />
                </div>
                <div className="flex items-center gap-2">
                  <Filter className="w-4 h-4 text-slate-400" />
                  <select 
                    className="bg-white border border-slate-200 rounded-lg text-sm px-3 py-2 outline-none"
                    value={filtroTipo}
                    onChange={(e) => setFiltroTipo(e.target.value)}
                  >
                    <option value="Todos">Todos los tipos</option>
                    <option value="Investigador">Investigadores</option>
                    <option value="Empresa">Empresas/Consultoras</option>
                  </select>
                </div>
              </div>
            </div>
          </div>

          <div className="overflow-x-auto">
            <table className="w-full text-left border-collapse">
              <thead>
                <tr className="bg-slate-50 text-slate-500 text-xs uppercase tracking-wider">
                  <th className="px-6 py-4 font-semibold border-b border-slate-100">Tipo</th>
                  <th className="px-6 py-4 font-semibold border-b border-slate-100">Entidad</th>
                  <th className="px-6 py-4 font-semibold border-b border-slate-100">Nombre</th>
                  <th className="px-6 py-4 font-semibold border-b border-slate-100">Área Específica</th>
                  <th className="px-6 py-4 font-semibold border-b border-slate-100">Ciudad</th>
                  <th className="px-6 py-4 font-semibold border-b border-slate-100">Nota</th>
                </tr>
              </thead>
              <tbody className="divide-y divide-slate-100">
                {filteredRegistros.map((reg, idx) => (
                  <tr key={idx} className="hover:bg-blue-50/30 transition-colors">
                    <td className="px-6 py-4">
                      <span className={`px-2 py-1 rounded-md text-[10px] font-bold uppercase ${
                        reg.tipo === 'Investigador' ? 'bg-indigo-100 text-indigo-700' : 'bg-amber-100 text-amber-700'
                      }`}>
                        {reg.tipo}
                      </span>
                    </td>
                    <td className="px-6 py-4 font-medium text-slate-700">{reg.entidad}</td>
                    <td className="px-6 py-4 text-slate-900">{reg.apellido}, {reg.nombre}</td>
                    <td className="px-6 py-4 text-sm text-slate-600">{reg.area}</td>
                    <td className="px-6 py-4 text-sm text-slate-500">{reg.ciudad}</td>
                    <td className="px-6 py-4 text-xs italic text-slate-400">{reg.nota}</td>
                  </tr>
                ))}
              </tbody>
            </table>
            {filteredRegistros.length === 0 && (
              <div className="py-20 text-center text-slate-400">
                No se encontraron resultados para tu búsqueda.
              </div>
            )}
          </div>
        </section>
      </main>

      {/* Footer */}
      <footer className="bg-slate-900 text-slate-400 py-12">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
          <p className="text-sm mb-4 italic">"El futuro no es un lugar al que vamos, sino uno que estamos creando."</p>
          <div className="flex justify-center gap-6 mb-8 text-xs font-mono uppercase tracking-widest">
            <span>Investigación Granular</span>
            <span>Mapeo Regional</span>
            <span>Gestión del Conocimiento</span>
          </div>
          <p className="text-xs">© 2024 Observatorio de Prospectiva - Versión Modular 1.0</p>
        </div>
      </footer>
    </div>
  );
};

export default App;
